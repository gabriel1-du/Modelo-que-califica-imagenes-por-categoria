# notebooks/

Notebooks de **experimentación** (versiones previas, pruebas de arquitecturas, alternativas de hiperparámetros).

| Archivo | Qué es |
| :--- | :--- |
| `1. NotebookMLPLay3.ipynb` | MLP 3 capas, 100 % de `seg_train` |
| `2. notebookMLPPat2Lay3.ipynb` | MLP 3 capas, variante de hiperparámetros |
| `3. notebookCorregidoMLP3Capas(FRANCISCO) (1).ipynb` | MLP 3 capas con split 80/20 |
| `4. Presentacion.ipynb` | **Notebook principal de entrega** |

> Las rutas de `dataset/`, `copy/`, `images/` y `models/` son relativas a la **raíz del proyecto**.
> El notebook principal resuelve eso solo con la variable `RAIZ` (celda 16): funciona con el kernel
> lanzado desde la raíz o desde esta carpeta. Los notebooks `1.`–`3.` asumen cwd en la raíz.
